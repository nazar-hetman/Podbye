# ── Python ───────────────────────────────────────────────
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.eggs/
build/
dist/

# ── Virtual environments ─────────────────────────────────
.venv/
venv/
env/

# ── Test / tooling caches ────────────────────────────────
.pytest-tmp/
.pytest_cache/
.mypy_cache/
.ruff_cache/
.coverage
htmlcov/

# ── Runtime artifacts ────────────────────────────────────
crash.log
*.log
semantic_entity_export.json

# Ad-hoc scratch/report output from local debugging runs.
scratch_*

# ── Archives / design exports ────────────────────────────
*.tar
*.zip

# ── Editor backups ───────────────────────────────────────
*.backup
*.bak
*.orig

# ── IDE / OS ─────────────────────────────────────────────
.idea/
.vscode/
Thumbs.db
desktop.ini
.DS_Store

# Design mockups live outside the repo (../Vigil-design-archive) — 34 MB of
# PNGs that no build or test reads. That sibling folder keeps its old name:
# it is the user's own directory, not something the rename reaches.
Design/
podbye/
vigil/
Podbye_design.html
Vigil_design.html
podbye_design.tar
vigil_design.tar

# Scratch output
crash.log
scratch_tr_report.json

# Release artifacts — produced by the build, published to GitHub Releases,
# never committed.
SHA256SUMS.txt
installer/Output/
